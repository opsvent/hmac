---
kind: pipeline
type: docker
trigger:
  ref:
    - refs/heads/master
    - refs/pull/**
    - refs/tags/*
steps:
  - name: install
    image: node
    commands:
      - npm ci
  - name: lint
    image: node
    commands:
      - npm run lint
  - name: build
    image: node
    commands:
      - npm run build
  - name: publish
    image: node
    when:
      status:
        - success
      event:
        - tag
    environment:
      NPM_TOKEN:
        from_secret: OPSVENT_NPM
    commands:
      - npm set registry https://registry.npmjs.org
      - npm set //registry.npmjs.org/:_authToken $NPM_TOKEN
      - npm publish
---
kind: signature
hmac: ef91e3dcc5fcc2327850b0fce4019d8186429ea63b16f3df1a8843e0754ee3da

...
